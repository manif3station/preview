# 2026-05-27 - Initial Preview Skill

The first `preview` release packages a DD-native local file browser and preview pane.

- added `dashboard preview.files [directory]` to persist the chosen local root
- added `/app/preview` with bookmark-backed Ajax browsing and preview logic
- added inline preview support for text, images, pdf, audio, and video files
- added Docker-verified Perl and Playwright smoke tests
- added the required README, docs, ticket records, `Changes`, `.env`, and MIT `LICENSE`
