# SOW

## ID

`SOW-014`

## Title

Create the `preview` skill as a DD-native local file browser and preview pane.

## Goal

Ship `skills/preview` as an isolated skill repo that lets `dashboard preview.files` bind the current local directory into a Developer Dashboard browser page with inline previews for text, images, pdf, audio, and video files.
