# EPIC-288

## Title

Repair installed `preview` Ajax module loading.

## Goal

Make the shipped `preview` DD page load its own Perl modules reliably after skill installation so the browser route works without manual `@INC` changes.

## Status

Done
