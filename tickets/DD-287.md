# DD-287

## Title

Build the `preview` local directory browser skill.

## Method

TDD

## Acceptance

- `dashboard preview.files [directory]` stores the selected local directory, defaulting to the current working directory
- the skill exposes a `/app/preview` DD page backed by local bookmark Ajax workers
- the page lists files and subdirectories from the stored root
- the page previews text, images, pdf, audio, and video files inline
- the page rejects directory traversal outside the selected root
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `preview` repo

## Status

Done
