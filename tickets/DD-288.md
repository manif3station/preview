# DD-288

## Title

Fix the `preview` installed Ajax lib bootstrap.

## Method

TDD

## Acceptance

- the installed `preview` Ajax workers load `Preview::Directory` and `Preview::State` from the skill's own `lib/` directory
- opening `http://127.0.0.1:7890/app/preview` no longer fails with `Can't locate Preview/Directory.pm`
- the copied Ajax worker files and the embedded bookmark Ajax blocks stay aligned
- Docker tests and coverage pass at `100%`
- `cover_db` is cleaned
- `README.md`, `docs/`, `Changes`, and `tickets/` are updated
- release commit and push complete from the `preview` repo

## Status

Done
